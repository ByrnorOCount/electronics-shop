# Make a copy of this file named .env and fill in your actual credentials.
# DO NOT commit the .env file to version control.

# =================================================================
#               CORE APPLICATION SETTINGS
# =================================================================
NODE_ENV=development
PORT=3001
# The base URL of your frontend application (used for social login redirects, etc.).
BACKEND_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173

# =================================================================
#               DATABASE (PostgreSQL)
# =================================================================
DB_CLIENT=pg
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user # usually postgre
DB_PASSWORD=your_db_password # the password you set during PostgreSQL installation
DB_DATABASE=electronics_shop
 
# =================================================================
#               SECURITY & AUTHENTICATION
# =================================================================
# Used for signing JSON Web Tokens (JWT). Use a long, random, and secret string.
JWT_SECRET=a_very_long_and_secure_secret_for_jwt_generation
JWT_EXPIRES_IN=24h # e.g., 30d, 24h, 1h
 
# =================================================================
#               THIRD-PARTY SERVICE INTEGRATIONS
# =================================================================
 
# --- Stripe Payment Gateway ---
# Get these from your Stripe Dashboard.
# https://dashboard.stripe.com/apikeys, use the test keys for development.
# Get these from your Stripe Dashboard: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
# This is used to verify incoming webhooks from Stripe. Get it from installing Stripe CLI -> stripe login -> stripe listen --forward-to http://localhost:3001/api/orders/webhook
STRIPE_WEBHOOK_SECRET=whsec_...
# **Public key goes in frontend .env**
# STRIPE_PUBLISHABLE_KEY=pk_test_... 
 
# --- Email Service (for Production) ---
# Used by Nodemailer. For development, a temporary Ethereal account is used automatically.
# For production, use a real SMTP provider like SendGrid, Mailgun, etc.
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email_username
EMAIL_PASS=your_email_password
 
# --- Google OAuth 2.0 ---
# Get these from the Google Cloud Console.
# https://console.cloud.google.com/, create a new project, top left 3 bars -> APIs & Services -> Credentials, create OAuth (2.0) Client IDs, set Authorized redirect URIs to http://localhost:3001/auth/google/callback, then get Client ID and Client Secret.
# Get these from the Google Cloud Console. Authorized redirect URI: http://localhost:3001/api/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
 
# --- Facebook Login ---
# Get these from the Meta for Developers dashboard.
# https://developers.facebook.com/apps/, create a new app, pick Facebook Login use case, go to App Settings -> Basic to find App ID and App Secret (facebook automatically allows local calls but you can append http://localhost:3001/auth/facebook/callback to authorized redirect URIs in Facebook Login settings).
# Get these from the Meta for Developers dashboard. Authorized redirect URI: http://localhost:3001/api/auth/facebook/callback
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret

# =================================================================
#               SHIPPING & CURRENCY
# =================================================================

# --- Giao Hàng Tiếp Kiện (GHTK) ---
# TBA
