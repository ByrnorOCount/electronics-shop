 # Make a copy of this file named .env and fill in your actual credentials.
 # DO NOT commit the .env file to version control.
 
 # =================================================================
 #               CORE APPLICATION SETTINGS
 # =================================================================
 # The current environment mode. Vite automatically sets this based on the command used (e.g., 'development' for `npm run dev`).
 # You generally don't need to explicitly set this here unless overriding for specific build scenarios.
 NODE_ENV=development
 
 # =================================================================
 #               THIRD-PARTY SERVICE INTEGRATIONS
 # =================================================================
 
 # --- Backend API ---
 # The base URL for your backend API.
 VITE_API_BASE_URL=http://localhost:3001/api
 
 # --- Stripe Payment Gateway ---
 # Your Stripe Publishable Key. This is safe to expose in client-side code.
 VITE_STRIPE_PUBLIC_KEY=pk_test_...
