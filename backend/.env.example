# Make a copy of this file named .env and fill in your actual credentials.
# DO NOT commit the .env file to version control.

NODE_ENV=development

# --- General Application Settings ---
PORT=3001

# --- Database Connection (PostgreSQL) ---
DB_CLIENT=pg
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_DATABASE=electronics_shop

# --- Security & Session Management ---
# Used for signing JSON Web Tokens (JWT). Use a long, random string.
JWT_SECRET=this_is_salt_for_json_web_token_generation
JWT_EXPIRES_IN=24h # e.g., 30d, 24h
# Used for signing session cookies. Use a different long, random string.
SESSION_SECRET=this_is_a_strong_secret_for_session_management
# Used for CSRF protection. Must be a random string of at least 32 characters.
CSRF_SECRET=a_very_strong_32_secret_for_csrf

# --- Stripe Payment Gateway ---
# Get these from your Stripe Dashboard.
# https://dashboard.stripe.com/apikeys, use the test keys for development.
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# --- Google OAuth 2.0 ---
# Get these from the Google Cloud Console.
# https://console.cloud.google.com/, create a new project, top left 3 bars -> APIs & Services -> Credentials, create OAuth (2.0) Client IDs, set Authorized redirect URIs to http://localhost:3001/auth/google/callback, then get Client ID and Client Secret.
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# --- Facebook Login ---
# Get these from the Meta for Developers dashboard.
# https://developers.facebook.com/apps/, create a new app, pick Facebook Login use case, go to App Settings -> Basic to find App ID and App Secret (facebook automatically allows local calls but you can append http://localhost:3001/auth/facebook/callback to authorized redirect URIs in Facebook Login settings).
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret

# --- Frontend URL ---
# The base URL of your frontend application (used for social login redirects).
FRONTEND_URL=http://localhost:5173